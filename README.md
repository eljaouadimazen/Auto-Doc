#  Auto-Doc — eljaouadimazen/Auto-Doc

> Documentation auto-generated by [Auto-Doc](https://github.com/eljaouadimazen/Auto-Doc) on 2026-03-23 13:49:18

---

**Auto-Doc Documentation**
==========================

### Project Overview

Auto-Doc is a secure AI-powered documentation generator for GitHub repositories. It fetches a public repository, analyzes its code structure using a custom regex-based AST parser, sanitizes any sensitive data, and generates professional documentation using the Groq LLM API.

The primary audience for Auto-Doc appears to be developers and project maintainers who want to automate the documentation process for their GitHub repositories.

### Architecture

Auto-Doc uses a three-layer architecture:

1.  **Local Processing Layer**: Runs entirely on the server, responsible for fetching repository content from GitHub, parsing code structure, detecting and redacting sensitive data, and building structured LLM prompts.
2.  **Cloud-based AI Generation Layer**: Utilizes the Groq LLM API to generate professional documentation based on the structured prompts.
3.  **Automated Publishing Layer**: Publishes the generated documentation.

The technologies used in Auto-Doc include:

*   Node.js with Express
*   `@octokit/rest` for GitHub API interactions
*   Custom `ASTParserService` for parsing code structure
*   Groq LLM API for AI-powered documentation generation

### Technologies & Dependencies

The following dependencies are inferred from the code:

*   `@octokit/rest`: GitHub API client
*   `dotenv`: Environment variable management
*   `ejs`: Templating engine
*   `express`: Web framework
*   `simple-git`: Git interactions
*   `nodemon`: Development server

### API Reference

Although the code does not provide an explicit API reference, the following routes can be inferred:

*   `/`: Web interface for manual pipeline execution
*   `/docs`: Generated documentation

The purpose of these routes is to expose the pipeline as explicit steps, allowing users to inspect intermediate results before proceeding.

### Key Modules

The following modules are identified:

*   `app.js`: Main application entry point
*   `ASTParserService`: Custom service for parsing code structure
*   `SanitizerService`: Service for detecting and redacting sensitive data
*   `scripts/semantic-diff.js`: Script for performing AST-level comparisons between commits

### Environment Configuration

The following environment variables are required:

*   `GITHUB_TOKEN`: GitHub API token
*   `GROQ_LLM_API_KEY`: Groq LLM API key

These variables are used to authenticate interactions with the GitHub API and the Groq LLM API.

### Security Notes

Auto-Doc has a security model that aims to prevent sensitive data from reaching the Groq LLM API. The `SanitizerService` runs two passes to detect and redact sensitive data:

1.  Once on the raw markdown blob returned by GitHub
2.  Once per-file before building the LLM prompt

The service uses 21 built-in detection patterns to identify sensitive data, including API keys, secret keys, access tokens, and passwords.

### Setup & Usage

To install and run Auto-Doc, follow these steps:

1.  Clone the repository: `git clone https://github.com/eljaouadimazen/Auto-Doc.git`
2.  Install dependencies: `npm install`
3.  Set environment variables: `GITHUB_TOKEN` and `GROQ_LLM_API_KEY`
4.  Run the application: `node app.js`

### Uncertainties

The following aspects cannot be determined from the code alone:

*   The exact implementation of the `ASTParserService` and `SanitizerService`
*   The structure and content of the generated documentation
*   The specifics of the Groq LLM API integration
*   The handling of errors and edge cases

These uncertainties highlight the need for additional documentation or code review to fully understand the implementation details of Auto-Doc.

---

*Generated with Auto-Doc — secure AI-powered repository documentation*
