#  Auto-Doc — eljaouadimazen/Auto-Doc

> Documentation auto-generated by [Auto-Doc](https://github.com/eljaouadimazen/Auto-Doc) on 2026-03-18 13:25:09

---

Auto-Doc Project Documentation
============================
## Project Overview

Auto-Doc is a secure AI-powered documentation generator for GitHub repositories. It fetches a public repository, analyzes its code structure using a custom regex-based AST parser, sanitizes any sensitive data, and generates professional documentation using the Groq LLM API.

The primary audience for this project appears to be developers and maintainers of GitHub repositories who require automated documentation generation while ensuring the security and confidentiality of their codebase.

## Architecture

The Auto-Doc system employs a three-layer architecture:

1.  **Local Processing Layer**: Runs entirely on the server, responsible for fetching repository content from GitHub via the Octokit REST API, parsing code structure, detecting and redacting sensitive data, building structured LLM prompts, and logging sanitization events.
2.  **Cloud-based AI Generation Layer**: Utilizes the Groq LLM API to generate professional documentation based on the structured prompts.
3.  **Automated Publishing Layer**: Although not explicitly detailed in the provided code, this layer is implied to handle the publication of the generated documentation.

## Technologies & Dependencies

The project relies on the following technologies and dependencies:

*   Node.js / Express for server-side processing
*   `@octokit/rest` for interacting with the GitHub API
*   Custom `ASTParserService` for parsing code structure
*   `dotenv` for environment variable management
*   `ejs` for templating
*   `simple-git` for Git operations
*   `nodemon` for development purposes
*   Groq LLM API for AI-powered documentation generation

## API Reference

Although the API reference is not explicitly provided, the following routes can be inferred based on the project's functionality:

*   **GET /docs**: Retrieves the generated documentation for a repository.
*   **POST /generate**: Triggers the documentation generation process for a specified repository.
*   **GET /health**: Checks the health and status of the Auto-Doc service.

## Key Modules

The following modules are crucial to the project's functionality:

*   `app.js`: The main application entry point, responsible for setting up the Express server and routing.
*   `ASTParserService`: A custom service for parsing code structure using regex-based AST parsing.
*   `SanitizerService`: Responsible for detecting and redacting sensitive data from the repository content.
*   `LLMService`: Handles the interaction with the Groq LLM API for generating documentation.

## Environment Configuration

The project requires the following environment variables to be configured:

*   `GITHUB_TOKEN`: A GitHub token for authenticating API requests.
*   `GROQ_LLM_API_KEY`: An API key for the Groq LLM API.
*   `REPOSITORY_OWNER` and `REPOSITORY_NAME`: The owner and name of the repository to generate documentation for.

## Security Notes

The project prioritizes security through the following measures:

*   Sensitive data detection and redaction using built-in regex patterns (21 patterns) and custom rules.
*   Logging of sanitization events for auditability.
*   Use of environment variables for storing sensitive credentials.

However, the security of the project also depends on the security of the dependencies and the Groq LLM API.

## Setup & Usage

To set up and use the Auto-Doc project, follow these steps:

1.  Clone the repository and install the dependencies using `npm install`.
2.  Configure the required environment variables.
3.  Start the server using `npm start`.
4.  Trigger the documentation generation process by sending a POST request to the `/generate` endpoint.

## Uncertainties

The following aspects of the project cannot be determined from the code alone:

*   The exact implementation details of the `ASTParserService` and `SanitizerService`.
*   The structure and content of the generated documentation.
*   The specifics of the Groq LLM API integration and its requirements.
*   The project's handling of errors, edge cases, and unexpected input.

---

*Generated with Auto-Doc — secure AI-powered repository documentation*
